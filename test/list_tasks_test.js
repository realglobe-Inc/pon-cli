/**
 * Test case for listTasks.
 * Runs with mocha.
 */
'use strict'

const listTasks = require('../lib/list_tasks.js')
const assert = require('assert')
const co = require('co')

describe('list-tasks', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('List tasks', () => co(function * () {

  }))
})

/* global describe, before, after, it */
